/* ============================================================
   HANGAR WORLD — CAMPEONATO FC26
   js/config.js

   Só ficam aqui a URL do projeto e a chave ANÔNIMA (pública) do
   Supabase — são seguras de expor no navegador, pois toda escrita
   é bloqueada por Row Level Security e só acontece através das
   rotas /api/admin/* (que usam a service role key, essa sim secreta
   e guardada nas variáveis de ambiente da Vercel).

   Troque os valores abaixo pelos do seu projeto Supabase
   (Project Settings → API).
   ============================================================ */
window.SUPABASE_URL = 'https://jinfxvklfkhoevrihixl.supabase.co';
window.SUPABASE_ANON_KEY = 'sb_publishable_8yIV2TjrvHbZNXStNRHxEg_MwMzW02N';
